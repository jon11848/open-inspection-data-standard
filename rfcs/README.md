# Requests for Comments

Use an RFC for material or potentially breaking changes. Copy `0000-template.md`, assign the next number, and open a pull request. An RFC should remain in `proposed` status during review and should link to the final decision record when resolved.
